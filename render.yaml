services:
  - type: web
    name: music-library
    env: node
    plan: free
    branch: main
    buildCommand: "npm install"
    startCommand: "node server.js"
    repo: YOUR_GITHUB_USERNAME/YOUR_REPO_NAME
